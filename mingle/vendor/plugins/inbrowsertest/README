Rails-web-test
==============

Rails_web_test is yet another Selenium plugin for Rails...

It's a bit easier to install than Jonas Bengtsson Selenium For Rails plugin and a bit simpler and more self-contained. 